# Data

Scenario-input templates for the decision model.

The study is scenario-based. Values labeled as assumptions or modeled inputs must remain distinguishable from measured observations.

When adding empirical data, document its provenance and measurement context.
